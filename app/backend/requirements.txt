fastapi>=0.115
uvicorn[standard]>=0.30
onnxruntime>=1.19
tokenizers>=0.20
numpy>=1.26
pydantic>=2.7
# huggingface-hub only needed if loading the model via MODEL_REPO (not for bundled model)
huggingface-hub>=0.24
