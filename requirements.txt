# Core (local, Python 3.13)
# torch: install separately with CUDA index (see README quickstart)
transformers>=4.44
datasets>=2.20
accelerate>=0.33
scikit-learn>=1.5
pandas>=2.2
numpy>=1.26
pyyaml>=6.0
psutil>=6.0
sentencepiece
protobuf

# Augmentation (Gemini free tier)
google-generativeai>=0.7

# Demo
gradio>=4.40

# 4-bit local inference (CPU/GPU GGUF)
llama-cpp-python>=0.2.90

# --- Kaggle-only (do NOT install locally; T4 notebook installs these) ---
# peft>=0.12
# bitsandbytes>=0.43
# trl

# --- Optional / may not build on Windows+Py3.13: run transliteration on Kaggle instead ---
# ai4bharat-transliteration
