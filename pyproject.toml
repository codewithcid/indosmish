[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "indosmish"
version = "0.1.0"
description = "Tri-class code-mixed Malayalam-English smishing detection benchmark"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
